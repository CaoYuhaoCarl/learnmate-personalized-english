[project]
name = "english-coach"
version = "0.1.0"
description = "ADK v2 workflow for English coaching feedback from ./submissions/."
requires-python = ">=3.10"
dependencies = [
    "google-adk",
    "pydantic>=2",
]
